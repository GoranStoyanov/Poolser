# macOS
.DS_Store
.AppleDouble
.LSOverride

# SPM / Swift build artifacts
.build/
*.o
*.d

# Xcode
*.xcuserstate
xcuserdata/
DerivedData/
*.xcworkspace/xcuserdata/
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3

# Xcode-generated schemes (keep shared ones if added manually)
*.xcscheme

# CocoaPods (not used, but just in case)
Pods/

# Local signing config (copy CodeSigning.xcconfig.sample → CodeSigning.xcconfig)
Poolser/CodeSigning.xcconfig

# Environment / secrets
.env
*.pem
*.p12
*.mobileprovision
*.provisionprofile
ExportOptions.plist

# Xcode code signing / archive intermediates
*.ipa
*.xcarchive

# fastlane (if added later)
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/
fastlane/test_output/
